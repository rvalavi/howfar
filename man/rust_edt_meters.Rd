% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{rust_edt_meters}
\alias{rust_edt_meters}
\title{Exact Euclidean distance transform returning distance in \strong{metres}.}
\usage{
rust_edt_meters(
  mask,
  nrows,
  ncols,
  n_cores,
  xmin,
  ymax,
  xres,
  yres,
  lonlat,
  m_per_unit
)
}
\arguments{
\item{mask}{Numeric vector, row-major, length nrows*ncols (>0.5 = source).}

\item{nrows}{Integer. Number of raster rows.}

\item{ncols}{Integer. Number of raster columns.}

\item{n_cores}{Integer. Threads to use; 0 = rayon default, 1 = serial.}

\item{xmin}{Double. Left edge (x) of the raster extent.}

\item{ymax}{Double. Top edge (y) of the raster extent.}

\item{xres}{Double. Cell size in x.}

\item{yres}{Double. Cell size in y.}

\item{lonlat}{Logical. If true, use Haversine (lon/lat in degrees).}

\item{m_per_unit}{Double. Metres per CRS linear unit (projected only).}
}
\value{
Numeric vector of distances in metres, row-major.
}
\description{
Finds, for each cell, the planar-Euclidean nearest source cell (via the
feature transform), then measures the metric distance between cell centres:
\code{haversine} when \code{lonlat} is true, otherwise \code{euclidean} scaled by
\code{m_per_unit} (metres per CRS linear unit). Cell centres are derived from the
grid geometry: cell \verb{(r, c)} has centre \verb{(xmin + (c + 0.5) * xres, ymax - (r + 0.5) * yres)}. Cells with no source return infinity.
}
