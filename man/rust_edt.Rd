% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{rust_edt}
\alias{rust_edt}
\title{Exact Euclidean distance transform of a binary mask.}
\usage{
rust_edt(mask, nrows, ncols, n_cores)
}
\arguments{
\item{mask}{Numeric vector, row-major, length nrows*ncols. Source cells
have value > 0.5; background cells have value <= 0.5 (or NA, but the
R wrapper should replace NAs first).}

\item{nrows}{Integer. Number of raster rows.}

\item{ncols}{Integer. Number of raster columns.}

\item{n_cores}{Integer. Number of threads to use. 0 means "use rayon's
default" (all logical cores). Positive values cap parallelism at that
thread count; 1 runs serially. Negative values are treated as 0.}
}
\value{
Numeric vector of distances, row-major, same length as \code{mask}.
}
\description{
Returns the Euclidean distance (not squared) from each cell to the
nearest source cell, in cell units. R-callable entry point.
}
